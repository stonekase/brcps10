package com.sourcard.helpers;

import javax.servlet.AsyncContext;
import javax.servlet.http.HttpSession;
public class analyseMenus {
	
	public static void CustomerMenu(long msisdn,String menuName,HttpSession session,AsyncContext asyncCtx)
	{
		
	}
	public static void SubscriptionMenu(long Msisdn,String menuName,HttpSession session,AsyncContext asyncCtx)
	{
		
	}
}
